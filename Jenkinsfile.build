pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                sh "chmod +x -R ${env.WORKSPACE}"
                sh './file2.sh'
            }
        }
    }
}
