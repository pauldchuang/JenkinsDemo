def runScript{
    sh "chmod +x ./file2.sh"
    sh "chmod +x ./file1.sh"
    sh './file2.sh'   
}
pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            environment{
                
            }
            steps {
                script {runScript()}
            }
        }
    }
}
