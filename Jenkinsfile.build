def runScript = {
    sh "chmod +x ./file2.sh"
    sh "chmod +x ./file1.sh"
    sh "./file1.sh ${env.PROD1}"
}
pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            environment{
                PROD1 = """abc
                def
                ghi
                jkl"""
            }
            steps {
                script {runScript()}
            }
        }
    }
}
